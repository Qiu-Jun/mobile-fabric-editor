// 首页底部tab
export enum TabItem {
  template = 'Template',
  text = 'Text',
  image = 'Image',
  bg = 'Background'
}

// 背景tab
export enum BgTabItem {
  color = 'Color',
  image = 'Image',
  clear = 'clear'
}

// 图片tab
export enum ImgTab {
  copy = 'Copy',
  image = 'Image',
  filters = 'Filters',
  layer = 'Layer',
  flip = 'Flip',
  opacity = 'Opacity',
  tuning = 'Tuning'
}

// 文本tab
export enum TextTab {
  copy = 'Copy',
  text = 'Text',
  font = 'Font',
  color = 'Color',
  style = 'Style',
  opacity = 'Opacity',
  tuning = 'Tuning',
  layer = 'Layer'
}
